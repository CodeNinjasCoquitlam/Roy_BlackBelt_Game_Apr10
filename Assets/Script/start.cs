using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class start : MonoBehaviour
{
    public static GMS chooseLives;
    // Start is called before the first frame update
    void Start()
    {
        GMS.startGame();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
